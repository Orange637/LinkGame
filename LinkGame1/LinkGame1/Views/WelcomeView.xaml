<UserControl x:Class="LinkGame1.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Background="SkyBlue"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <Grid>
        <TextBlock Width="80"
                   Height="30"
                   Margin="98,108,122,162"
                   Text="Nickname:" />
        <TextBox Width="80"
                 Height="30"
                 Margin="165,105,55,165"
                 Text="{Binding Nickname,
                                Mode=TwoWay}" />
        <Button Width="80"
                Height="25"
                Click="StartPlayCommand"
                Content="Start Play" />
    </Grid>
</UserControl>
